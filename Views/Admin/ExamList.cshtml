@model IEnumerable<StudentInformationSystem.Models.Exams>

@{
    ViewBag.Title = "ExamList";
}

<h2>���԰����б�</h2>

@* --- �������� --- *@
@using (Html.BeginForm("ExamList", "Admin", FormMethod.Get, new { @class = "form-inline" }))
{
    <div class="form-group">
        <label>���ҿ���:</label>
        <input type="text" name="searchString" value="@ViewBag.CurrentFilter" class="form-control" placeholder="����γ������Եص�" />
    </div>
    <input type="submit" value="�� ��" class="btn btn-default" />
}
<br />

<p>
    @Html.ActionLink("�����¿���", "AddExam", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            �γ�����
        </th>
        <th>
            ����ʱ��
        </th>
        <th>
            ���Եص�
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Courses.CourseName)
            </td>
            <td>
                @item.StartTime.ToString("yyyy-MM-dd HH:mm") - @item.EndTime.ToString("HH:mm")
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.ActionLink("�༭", "EditExam", new { id = item.ExamID }) |
                @Html.ActionLink("����", "DetailsExam", new { id = item.ExamID }) |
                @Html.ActionLink("ɾ��", "DeleteExam", new { id = item.ExamID })
            </td>
        </tr>
    }

</table>
